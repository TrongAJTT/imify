/**
 * Shared PostCSS config for all apps.
 * Requires tailwindcss and autoprefixer to be installed in the consuming app.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
}
