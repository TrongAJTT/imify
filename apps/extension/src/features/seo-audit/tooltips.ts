export const SEO_AUDIT_TOOLTIPS = {
  inspectAction: "Inspect",
  optimizeAction: "Optimize now",
  scanScopeLabel: "SEO Audit scan scope",
  scanScopeContent:
    `This feature can only scan the following elements:
    - Visible <img> and CSS background images
    - HTTP/HTTPS image URLs
    
    Otherwise, it will be skipped because they are protected assets.`,
} as const
